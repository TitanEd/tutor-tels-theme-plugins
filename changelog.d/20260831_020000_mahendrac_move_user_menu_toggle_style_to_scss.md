- [Improvement] Custom header user-menu toggle (`CustomHeaderUserMenuDropdown.jsx`, avatar/icon + name + chevron) no longer injects its CSS at runtime via a `<style>` tag. The `.tels-user-menu-toggle-*` rules now live in `tels-brand-openedx`'s `paragon/_header.scss`, using the same `var(--pgn-color-*, hex fallback)` design-token convention as the rest of that file, so the styling ships compiled with the header's other design tokens instead of being written to the DOM on every mount.
