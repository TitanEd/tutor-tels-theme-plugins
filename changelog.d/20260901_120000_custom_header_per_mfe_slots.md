- 💥[Improvement] Shared site header is `CustomHeader`, injected per MFE on the slot id that MFE actually mounts (`header.v1` for public insert-only; `header_desktop.v1`+`header_mobile.v1` for DesktopHeader apps; `header_learning.v1` for LearningHeader apps). Discussions/communications/ora-grading get that learning slot via a Tutor Dockerfile wrap at image build (`mfe-dockerfile-pre-npm-build-*`), not MFE git changes. Public no longer renders an empty HeaderSlot.
